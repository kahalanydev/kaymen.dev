# Generated OG images

Do not hand-edit. Regenerate with:

    node C:/KDEV/_tools/showcase/gen-og.mjs "C:/KDEV/Personal/Kaymen.Dev Site"

Source of truth is `content/projects.js` (and `content/stats.js` for the
default card's live count) in the site repo — add a case study there and re-run
this to get its card. 1200x630, committed and served statically so production
needs no image toolchain.

Design: the site's `--deep` (#16303d) with the one accent, Sora + Inter — the
same system as the site. Each card carries exactly ONE figure: at thumbnail size
in a feed, three competing numbers read as a brochure and none of them survives.
